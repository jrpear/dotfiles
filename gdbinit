# TODO use the glibc source distributed with your distro rather than some other
# random glibc of which the line numbers won't quite match what your program is
# using.

# seems most libc source paths have a preceding .., so we need to specify a subdir of glibc.
directory ~/repos/glibc/scripts

tui reg general
